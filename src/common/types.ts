export interface IOptions {
  top?: number;
  columns: number;
  rows: number;
  scale?: number;
  styles?: object;
  timeTooltip?: boolean;
  thumbnails: string[];
  seekBarId?: string;
  interval?: number;
}
